/**
 * Vibe Investing — 프론트엔드 진입점 (vanilla TS, esbuild 번들).
 *
 * 현재 상태: 스캐폴드 플레이스홀더. 실제 대시보드 UI(가이드 PART B)는
 * 후속 단계(§6.3 step 7)에서 구현한다. 차트는 경량 SVG 직접 렌더,
 * Inter/Roboto·보라 그라데이션 금지(가이드 §B-1).
 */

const APP = document.getElementById("app");
if (APP) {
  APP.textContent = "Vibe Investing — frontend scaffold. UI는 후속 단계에서 구현됩니다.";
}

export {};
